/**
 * @deprecated Tests moved to DeliverPhase.test.tsx — Deploy phase renamed to Deliver per SPEED phase names.
 */
import { describe, it, expect } from "vitest";

describe("DeployPhase (deprecated)", () => {
  it("replaced by DeliverPhase.test.tsx", () => {
    expect(true).toBe(true);
  });
});
