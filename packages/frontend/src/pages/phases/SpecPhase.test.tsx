/**
 * @deprecated Tests moved to SketchPhase.test.tsx — Spec phase renamed to Sketch in UI.
 */
import { describe, it } from "vitest";

describe("SpecPhase (deprecated)", () => {
  it("replaced by SketchPhase.test.tsx", () => {
    expect(true).toBe(true);
  });
});
